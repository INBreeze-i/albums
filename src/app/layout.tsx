import type { Metadata } from "next";
import "./globals.css";
import { Providers } from "./providers";
import { Navigation } from "@/components/Navigation";

export const metadata: Metadata = {
  title: "Albums Gallery - Beautiful Photo Collections",
  description: "Explore our curated collection of stunning photo albums featuring nature, architecture, and more.",
  keywords: ["photos", "albums", "gallery", "photography", "collection"],
  authors: [{ name: "Albums Gallery" }],
  openGraph: {
    title: "Albums Gallery",
    description: "Beautiful photo collections and albums",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="font-sans">
        <Providers>
          <div className="min-h-screen bg-background">
            <Navigation />
            <main className="container mx-auto px-4 py-6">
              {children}
            </main>
          </div>
        </Providers>
      </body>
    </html>
  );
}
